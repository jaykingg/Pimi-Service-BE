<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wisdomose.test_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_box_puppy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_puppy"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 로딩페이지 -->
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main, 1번째 페이지 -->
        <activity android:name=".MainActivity">

        </activity>

        <!-- controller, 3번째 페이지 -->
        <activity android:name=".ControllerActivity">

        </activity>

        <!--cmn, 2번쨰 페이지 -->
        <activity android:name=".CmnActivity">

        </activity>

    </application>
    <!-- AndroidManifest.xml 에 추가해야할 인터넷 사용 허가권 -->
    <uses-permission android:name="android.permission.INTERNET"/> <!--소켓 통신을 하기위해서 꼭 필요한 권한 -->

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/> <!--소켓 통신을 하기위한 부수적 권한-->

    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/> <!--소켓 통신을 하기위한 부수적 권한-->

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/> <!--소켓 통신을 하기위한 부수적 권한-->


</manifest>